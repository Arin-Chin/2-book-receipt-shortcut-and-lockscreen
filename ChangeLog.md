# 阅读小票 + 锁屏壁纸（阅读日票 / 墨痕壁纸 / 菜单留单台 / 随机图片）更新日志

> v2.7.0 起的逐版详细变更（每一版的动机、实现与取舍）写在补丁文件开头的 `-- 版本更新` 注释里，
> 本节只做阶段性汇总。

## [v2.7.16]

- **胶片票根 → 阅读日票**：移植独立补丁的 READ RECEIPT 渲染器（贝塞尔圆角 + 锯齿 + 撕口 + 伪条码 + 只读星级），样式键仍为 `film`，勾选池 / 旧配置迁移 / 锁屏降级逻辑零改动；日票不响应评分点击。
- **手势动作拆分改名**：`Book-RL：阅读摘要`（走勾选池 + 显示模式）与 `Book-RL：阅读日票`（恒显示日票，不推进轮换）。
- **新增第四种样式「随机图片」**：整屏显示 KOReader 默认图片文件夹里的随机图片（= 原生随机图片屏保效果），与其它样式同池轮流 / 随机，共用「背景图片显示方式」。
- **锁屏背景**（原「背景」）：只作用于锁屏；「透明」在锁屏兜底为不透明底色，永不露出当前阅读页；`居中（不缩放）` 在图片铺不满屏幕（会被票面完全遮住）或过大时自动改用「适应屏幕」。
- **墨痕「设备信息：…」栏可自定义输入**（留空 = 自动读取），并从墨痕子菜单上移到「摘要显示模式」下方；
- **一级菜单交互**：样式项（阅读日票 / 墨痕壁纸 / 菜单留单台 / 随机图片）支持**长按整行勾选 / 取消**（KOReader 的勾选框列固定在左侧、`TouchMenuItem` 没有右侧槽位，故用官方支持的 `hold_callback`）；
- **唤醒合并重绘已删除**（v2.7.10 引入、默认关）：实测唤醒刷新均由 KOReader 自身产生，补丁不再干预，只保留一行 `唤醒刷新计数` 诊断日志供两台设备对比；
- **阅读日票设置精简**：删除原版胶片遗留、对日票无效的「内容」与「设置休眠状态显示文字」（连同设置键、汉化与死代码）。
- **性能**：诗词/菜谱/名言改为首次使用时解析（不再阻塞 KOReader 启动）；底图缩放沿用 KOReader 的「拉伸上限」设置。
- **工程**：补齐沙箱单测（`spec/test_style_pool.js`）、`.gitignore` 与本文档；新增锁屏/唤醒日志便于排查。

## [v2.6.0]
- **菜单按摘要类型重构**：一级菜单改为 `胶片票根 / 墨痕壁纸 / 菜单留单台 / 摘要显示模式`。前三个样式菜单各带勾选框（点左侧方框切换是否纳入显示池，点文字进入该样式设置）；`摘要显示模式` 简化为「轮流显示 / 随机显示」，固定效果由“仅勾选一种样式”自然实现。旧配置自动派生勾选状态：旧 random/alternate → 全选，旧 fixed → 仅该样式，无需手动迁移。手势与锁屏改回共用同一显示池与模式（移除 v2.5.0 的锁屏独立键）。
- **胶片票根菜单**：内容 / 背景 / 封面缩放 / 休眠文字（封面缩放与休眠文字从“内容”子菜单提升为一级项）。
- **背景随机图片**：改用系统默认的自定义图片文件夹（屏保设置 `screensaver_dir`），不再使用补丁自建的 `book_receipt_background` 目录。
- **完全去除小花（✿）按钮**：胶片票根与墨痕/菜单壁纸均不再显示小花，不再广播 `ShowReadingInsightsPopup`。
- **墨痕壁纸设备栏实时化**：设备型号优先 `Device:info()`（回退 `Device.model`），分辨率与 DPI 每次渲染实时读取；并在墨痕壁纸设置内新增只读设备信息行，每次打开菜单刷新。

## [v2.5.4]
- 修复胶片票根(film)锁屏样式在无活动文档时错误回退为「书籍封面」屏保（GitHub issue #1）：锁屏时若未打开任何书籍，原逻辑会调用 `showFallbackScreensaver` 显示 lastfile 封面，造成样式突变；现改为降级渲染墨痕壁纸(inkstain)，既保留小票观感又避免露出书籍封面。其余两种样式（墨痕/菜单）本就基于全局阅读统计、无需活动文档，不受影响。

## [2026-08-09 维护性修复]
- 补丁健壮性修复（代码审查驱动，最小化修改，全部经 LuaJIT `loadfile` 语法校验通过）：
  - `_G.dofile` 覆写：`orig_dofile` 调用包 `pcall`，注入体整体 `pcall` + 幂等扫描 `br_marker`（菜单项已存在则跳过），杜绝上游菜单结构变动/重入时抛错阻断 UI、重复插项。
  - `Screensaver.show` 加 `_book_receipt_patched` 幂等守卫，防止重复包装。
  - 移除脆弱的相对资源路径 `./plugins/inkstain.koplugin`，仅保留 `DataStorage` 绝对路径。
  - 文件头声明修正为与实现一致（渲染不写盘、无定时器；加载期写字体、延迟广播；无网络）。
  - `logger.warn` 补 `LOG_TAG`（与另一处一致）；`onTap`/`onSwipe` 补 `return self:onClose()` 消费事件；`genMenuItem` 删未用死参 `separator`。

## [v2.5.1]
- 修复手势/锁屏样式分离未生效的问题：`Screensaver.show` 调用 `buildReceipt` 时漏传 `context` 参数，导致锁屏固定显示手势样式（锁屏“轮流出现”失效）。改为调用方解析一次 `style` 后直接传入，根治轮流模式被预检与渲染双重推进跳样式的隐患。

## [v2.5.0]
- **手势/锁屏样式独立配置**（非对称解耦）：手势沿用全局键（零改动），锁屏新增 `book_receipt_lockscreen_{mode,style,toggle_state}` 独立键，首次锁屏从旧全局键单向迁移（修复旧版仅开随机开关时迁移后随机偏好丢失的隐患）。
- 菜单拆分“手势调出样式 / 锁屏壁纸样式”双通道子菜单，并在帮助中注明两通道共享统计周期、书单数量、诗句轮换等设置。

## [v2.4.13]
- 修复菜单样式左上角“点单设备：设备：”双前缀问题：`getDeviceInfoString` 增加 `prefix` 入参，墨痕默认“设备：”，菜单传入“点单设备：”，消除调用点二次拼接。

## [v2.4.12]
- 诗词板块再次扩容：追加诗词第三版，剔除重复项，净增 100 条，总量精确 200 条；保留李贺“天若有情天亦老”独立短句。

## [v2.4.11]
- **内容全面扩容**：
  - 诗词板块替换为 100 条（第二版），名言追加至 200 条（第二版），菜谱追加至 250 道（第二版）。
  - 菜单样式右上角标题“菜单”改为“菜单：5份”。

## [v2.4.10]
- 诗词板块删减：陈毅《梅岭三章》替换为“此去泉台招旧部，旌旗十万斩阎罗。”和“取义成仁今日事，人间遍种自由花。”

## [v2.4.9]
- 墨痕“时长”与菜单“总计光临本店”左上角栏位统一换算为以“天”为单位显示（保留 1 位小数，<0.05 天显示“不足1天”）。

## [v2.4.8]
- 右下名言板块字号与左下诗词板块逐行完全一致（名言=诗句 `quote_size`，名人=朝代·作者 `meta_size`，出处=作品名 `meta_size`）；布局测量改用实际字号，第一行行距取两者较高者，防止长名言行换行压到名人行。

## [v2.4.7]
- 右下角名言出处行统一加书名号《出处》，与左侧诗词作品名行排版一致。

## [v2.4.6]
- 菜单样式特色/食材行不再按固定 18/26 字截断：新增 `truncateToWidth` 按像素宽度截断，充分利用表格右侧空间，同时以 `title_w` 为硬边界保证不与“数量”栏重叠。
- 右下角名言第一行按标准引用格式显示（中文引号 + 句号“名言。”）。

## [v2.4.5]
- **新增第三种样式「菜单样式」（menu）**：UI 布局与墨痕一致，表格改为 5 道菜（No.1/2 主菜 + No.3 汤菜 + No.4 饮品 + No.5 甜点），菜谱内嵌解析（分类字样不显示），三行换为 菜名/特色/食材，单价列与合计显示价格并带“元”（合计=5 道菜价格总和）。
- 文本替换：下单时间（仅当天日期）/点单设备/总计光临本店/用餐时间/菜单/留台单/Order Slip 等；右下角版权署名替换为名人名言三行（名言内嵌，右对齐并与左侧诗句三行水平对齐）。
- 轮流模式泛化为 N 样式顺序循环（film→inkstain→menu），首次调用回落首样式。

## [v2.4.4]
- 墨痕底部布局改为表格锚定：表格区恒按 5 本预留高度（无书/少书时留白、分隔线位置稳定），分隔线紧贴书单底部仅留小缝隙；底部区块整体上移、自顶向下紧凑排列，屏幕底部自然留白。

## [v2.4.3]
- 修复伪二维码成片同色问题：v2.4.2 的坐标线性哈希导致相邻格高度相关，产生一半黑一半白的大色块。改为顺序推进的 Park-Miller 伪随机序列逐格填充，输出天然迷宫状。

## [v2.4.2]
- 伪二维码内部迷宫化：以确定性哈希黑白散点取代线性取模斜条纹，并补充真实二维码时序图案（第6行/第6列交替条）；书单序号 `NO.` → `No.`。

## [v2.4.1]
- 6 寸屏优化：底部边距独立压缩、底部区块间距与汇总行间距收紧、表格行高按内容瘦身，墨痕书单在小屏设备上稳定显示 5 本；水平对齐与分离机制不变。

## [v2.4.0]
- 底部区域响应式重构：二维码/条码行与折线图日期标签行强制分离（先测量后分配），图表高度与间距全部随 scale 缩放，大/标准/小屏设备空间占比自适应。

## [v2.3.1]
- 版权署名回归右下角，与左下角诗句同一行（同一高度），高于朝代·作者与作品名行。

## [v2.3.0]
- 修复墨痕诗句不轮换（改为每次展示顺序推进一首）；尺寸单位“吋”改为“寸”。

## [v2.2.0]
- 墨痕页脚诗句库替换为 40 句经典诗词，单行改为三行显示（诗句 / 朝代·作者 / 作品名）。

## [v2.1.0]
- 新增样式“轮流出现”模式（与随机并列），手势调出与锁屏共享轮换序列。

## [v2.0.2]
- 墨痕样式去除“来源”栏；设备行显示真实设备型号/屏幕尺寸/分辨率/DPI。

## [v2.0.1]
- 修复墨痕书单仅显示 4 本（移除 1 分钟门槛）；书名列智能匹配屏幕宽度，书名可显示更长。

## [v2.0.0]
- 整合墨痕壁纸引擎，移除 6 种旧卡片样式，保留胶片票根，两样式同池 50% 随机。

## [v1.x]
- 初始版本：阅读小票（胶片票根 + 6 种卡片样式 + 花朵按钮），支持手势调出与锁屏。
